// Props will be a list of outfit Id
// map to it then render outfit cards by passing key, index, outfitid, info, handle functions (if needed)

import React from 'react';

const OutfitCarousel = (props) => {

    return (
        <div>Outfit Carousel</div>
    );
}

export default OutfitCarousel;